# 进阶待办事项应用 — 产品需求文档（PRD）

- 状态：供开发使用的 v1 需求基线
- 产品形态：适配桌面和手机浏览器的中文单页网页
- 对应技术设计：[TECH_DESIGN.md](TECH_DESIGN.md)

## 1. 目标与范围

面向希望在一台设备的浏览器中管理个人事务的用户。用户可以快速记录任务、按条件找到任务、查看整体完成进度；刷新页面后，原有任务仍可使用。

v1 包含任务与分类管理、搜索和组合筛选、整体统计、浏览器本地保存。v1 不包含账号、云端或跨设备同步、提醒、重复任务、协作和数据导入导出。数据保存在当前网页来源的当前浏览器中；清除网站数据或使用隐私浏览窗口可能导致数据丢失。

## 2. 页面与主要流程

首页从上到下展示：标题及“新增任务”入口、统计卡片、搜索框与筛选控件、任务列表，以及分类管理入口。窄屏时控件纵向排列，任务的主要操作仍可直接找到。新增和编辑使用同一表单；编辑时显示任务现有值。

1. 用户新建任务，填写标题，并按需填写描述、截止日期、优先级和分类；保存后，任务立即进入列表和统计。
2. 用户通过勾选完成状态、编辑或删除维护任务。删除任务前需确认；取消删除不改变数据。
3. 用户输入关键词，或选择分类、优先级、完成状态筛选。搜索与三个筛选条件同时生效，可单独清除条件。
4. 用户可以新增、重命名或删除分类。删除分类前说明受影响任务会转为“未分类”，并要求确认。

## 3. 任务规则

| 字段 | 规则 |
| --- | --- |
| 标题 | 必填；保存前去除首尾空白；长度 1–120 个字符。只有空白时提示错误。 |
| 描述 | 可选；纯文本；最多 2000 个字符。 |
| 截止日期 | 可选；仅日期，不含时分和提醒；可选择过去或未来日期。 |
| 优先级 | 低、中、高三级；新建时默认“中”。 |
| 分类 | 可选；一项任务最多属于一个分类；未选择时为“未分类”。 |
| 完成状态 | 新建时为未完成；完成与未完成可来回切换；完成的任务仍可编辑。 |

任务列表显示标题、完成状态、优先级、分类、截止日期；有描述时展示描述内容。点击编辑可修改上述任务字段。任务保存、完成状态切换、删除后，列表和统计立即更新。

默认排序：未完成任务在前；同一状态内按截止日期从近到远排列，无截止日期的任务排在有日期的任务之后；日期相同时按优先级高到低，再按创建时间新到旧排列。

## 4. 分类、搜索和筛选

- 分类名保存前去除首尾空白，长度 1–40 个字符；名称忽略大小写后不得重复。初始没有预设分类。重命名保持分类与任务的关联。
- 删除分类时，归属该分类的任务保留并转为“未分类”；若当前正在按该分类筛选，删除后切换到“全部分类”。
- 搜索同时检查标题和描述，输入关键词时实时更新列表；采用不区分大小写的包含匹配，无需输入完整标题。首尾空白不参与搜索，空搜索词表示不过滤。
- 分类筛选提供“全部分类”“未分类”及已创建的分类；优先级筛选提供“全部优先级”和三级选项；状态筛选提供“全部”“待办”“已完成”。默认均为“全部”。
- 筛选和搜索使用交集：任务需满足每个已设置的条件才显示。筛选只改变列表显示，不改变统计口径或保存的数据。
- 搜索或筛选后没有匹配任务时，显示“没有符合条件的任务”和清除筛选入口；完全没有任务时，显示新增任务引导。

## 5. 统计与反馈

统计卡片始终基于**全部任务**，不随搜索和筛选变化：

| 指标 | 计算方式 |
| --- | --- |
| 待办数 | 未完成任务数量 |
| 完成数 | 已完成任务数量 |
| 总数 | 待办数 + 完成数 |
| 完成率 | 完成数 ÷ 总数 × 100%，显示为整数百分比；总数为 0 时显示 0% |

表单验证失败时，在对应字段旁说明原因并保留输入。保存失败时给出可理解的错误提示，界面不得假装已经持久化。读取本地数据失败时停止任务修改，展示重试和确认后清空异常数据的恢复入口，避免无提示覆盖原数据。

## 6. 验收标准

1. 可新增仅有标题的任务；新任务默认为中优先级、未完成、未分类。空白标题或超出长度的内容不能保存，且有明确提示。
2. 可编辑任务全部可编辑字段；切换完成状态后，列表和全部任务统计同步变化；删除确认后任务消失，取消删除不影响任务。
3. 可新增、重命名、删除分类。重命名后原任务仍属于该分类；删除后相关任务保留为未分类。
4. 搜索能从标题和描述找到任务；分类、优先级、状态与搜索条件可以组合使用；清除条件后列表恢复。
5. 统计不受筛选影响；空任务列表显示待办 0、完成 0、总数 0、完成率 0%。
6. 新增、编辑、完成切换、分类变化和删除在刷新后保持一致。存储写入失败时提示错误，不把未保存的变化显示为已保存。
7. 桌面与手机宽度下，表单、筛选及列表可正常操作；输入框、按钮和确认对话框可通过键盘使用。
